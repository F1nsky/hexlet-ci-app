name: ci-app-test
on: push
jobs:
  build:
    runs-on: ubuntu-latest


    steps:
      - run: echo "Имя текущего воркфлоу: $GITHUB_WORKFLOW"W'
      - uses: actions/checkout@v2
        uses: actions/setup-node@v2
        with:
          node-version: '18.x'
          cache: 'npm'
      - run: make setup
      - run: make start
      - run: make test
      - run: make lint
